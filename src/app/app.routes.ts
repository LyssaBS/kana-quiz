import { Routes } from '@angular/router';
import { KanjiQuizComponent } from './features/kanji/kanji-quiz/kanji-quiz';

export const routes: Routes = [
  { path: '', component: KanjiQuizComponent },
];
